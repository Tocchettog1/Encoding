Steps:

- Mudar "name" em package.json
- Mudar "description" em package.json
- Instalar dependencias do projeto, base:
    "cors": 
    "dotenv"
    "express"
    "morgan"
    "sucrase"
    "mysql2" (padrao)
    "sequelize" (padrao)
    "sequelize-cli" (padrao)

- Instalar devDependencias do projeto, base:
    "nodemon"

- Checar porta que o servidor vai rodar em server.js.
- Revisar dados de conexao do BD em config/database.

- Excluir este arquivo.